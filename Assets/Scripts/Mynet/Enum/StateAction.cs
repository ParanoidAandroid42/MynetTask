namespace Enum
{
    /// <summary>
    /// for state's situation
    /// </summary>
    public enum StateAction
    {
        OnGame,
        OnMenu,
        OnRestart,
        FeatureButtonSetState
    }
}